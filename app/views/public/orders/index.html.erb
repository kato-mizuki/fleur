</br></br>
<div class="container">
  <div class="row">
    <span class="bg-secondary p-2 mb-1">注文履歴一覧</span>
  </div>
</div>
<div class="container">
  <div class="row">
    <table class="table table-bordered text-light">
      <thead>
        <tr>
          <th>Day</th>
          <th>Address</th>
          <th>Order Item</th>
          <th>Price</th>
          <th>Order Detail</th>
        </tr>
      </thead>
      <tbody>
      <% @orders.each do |order| %>
        <tr>
          <td><%= order.created_at.strftime('%Y/%m/%d') %></td>
          <td>
            <%= order.postal_code %></br>
            <%= order.address %></br>
            <%= order.name %>
          </td><td>
          <% order.order_details.each do |order_detail| %>
           <%= order_detail.item.name %></br>
          <% end %>
          </td>
          <td><%= order.price %></td>
          <td><%= link_to "Show", public_order_path(order.id), class: "btn btn-primary" %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
