</br><div class="container">
  <div class="row">
    <span class="p-2 mb-1 bg-secondary">注文履歴一覧</span>
  </div>
</div></br>
<div class="container">
  <div class="row">
    <table class="table col-8">
      <thead>
        <tr class="table-active">
          <th>購入日時</th>
          <th>購入者</th>
          <th>注文個数</th>
          <th></th>
        </tr>
      </thead>
      <tbody>

        <% @orders.each do |order| %>

        <tr>
          <td><%= link_to order.created_at.strftime('%Y/%m/%d %H:%M:%S'), admin_order_path(order.id) %></td>
          <td>
            <%= order.address_name %>
          </td><% order_amount = 0 %>
          <td><% order.order_details.each do |order_detail| %>

          <% order_amount += order_detail.item_amount %>
          <% end %>
          <%= order_amount %></td>
          <td></td>
        </tr>
        <% end %>
      </tbody>
    </table>

  </div>
</div>
<%= paginate @orders %>